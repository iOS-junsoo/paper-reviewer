# 방법론 시각화 개선 — 잔여 항목

> 원래 계획(B→A→C→D·E)은 **전부 완료·커밋됨** (`a09b44e` B+A, `2898a59` C+D+E, 2026-07-11).
> 완료 내역 상세는 두 커밋 메시지 참고. 이 문서는 보류 항목만 남긴다.

## 완료 요약 (한 줄씩)
- B raw LaTeX 근절: 지침 금지 + verify `raw_latex` 검사 + 오염 5편 외과 패치(LLM 0원, 재생성 불필요)
- A 원본 그림 대조: 방법론 탭 상단 접이식 `buildMethodOrigFigures`(레이지 로드·원문 점프)
- C 스테퍼↔강독 연동: `attachStepDeepLinks` — 토큰 매칭+순서 폴백, 강독 생성 직후 갱신
- D 실험실 조작 축 2개 권장(지침) · E 미도달 단계 opacity ≥0.45(지침+참조 9건 `.zone.dim`)

## 보류 (착수 조건 명시)

### T10 — 신경장/암시적 표현 전용 타입 신설
- **현상**: NeRF류 논문이 항상 T7 폴백으로 처리됨(품질은 준수하나 전용 골격이 아님).
- **착수 조건**: 신경장/암시적 표현(NeRF·SDF·Gaussian Splatting 등) 논문을 앞으로도
  자주 분석하게 될 때.
- **작업**: 참조 구현 `prompts/method_viz_refs/t10_*.html` 1건 제작(좌표→네트워크→렌더링
  적분 골격 + 광선 샘플링 실험실) + 지침서 타입 표에 T10 추가 + verify 통과 확인.
  참조 제작 비용이 커서(반나절+) 보류.

### Mask R-CNN 타입 재판정 (T1 → T2)
- **현상**: 본질은 "Faster R-CNN에 마스크 브랜치+RoIAlign 삽입"이라 T2(모듈 삽입)가
  더 정확하나, 현행 T1 산출물 품질이 좋아 재생성 가치 낮음.
- **착수 조건**: 해당 논문 method 섹션을 다른 이유로 재생성할 일이 생길 때 겸사겸사.
